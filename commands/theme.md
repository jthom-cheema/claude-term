# Switch ClaudeTerm Theme

Switch the active ClaudeTerm theme for Windows Terminal tab coloring.

Usage: `/theme <name>`

Available themes: default, ocean, neon, pastel, solarized, dracula

This runs: `claude-term theme use <name>`

$ARGUMENTS
